{
  "id": "week5-strings-01",
  "title": "Username Validator",
  "week": 5,
  "topic": "strings",
  "description": "This program checks that a username is between 3 and 15 characters, starts with a letter, and is not on a banned list.",
  "code": [
    { "line": 1, "text": "public class UsernameValidator {" },
    { "line": 2, "text": "  public static void main(String[] args) {" },
    { "line": 3, "text": "    String username = \"Player1\";" },
    { "line": 4, "text": "    String banned = \"admin\";" },
    { "line": 5, "text": "    boolean valid = true;" },
    { "line": 6, "text": "" },
    { "line": 7, "text": "    if (username.length < 3 || username.length > 15) {", "bugId": "b1" },
    { "line": 8, "text": "      System.out.println(\"Username must be 3-15 characters.\");" },
    { "line": 9, "text": "      valid = false;" },
    { "line": 10, "text": "    }" },
    { "line": 11, "text": "" },
    { "line": 12, "text": "    if (!Character.isLetter(username.charAt(1))) {", "bugId": "b2" },
    { "line": 13, "text": "      System.out.println(\"Must start with a letter.\");" },
    { "line": 14, "text": "      valid = false;" },
    { "line": 15, "text": "    }" },
    { "line": 16, "text": "" },
    { "line": 17, "text": "    if (username == banned) {", "bugId": "b3" },
    { "line": 18, "text": "      System.out.println(\"That username is not allowed.\");" },
    { "line": 19, "text": "      valid = false;" },
    { "line": 20, "text": "    }" },
    { "line": 21, "text": "" },
    { "line": 22, "text": "    if (valid) {" },
    { "line": 23, "text": "      System.out.println(\"Welcome, \" + username + \"!\");" },
    { "line": 24, "text": "    }" },
    { "line": 25, "text": "  }" },
    { "line": 26, "text": "}" }
  ],
  "bugs": [
    {
      "id": "b1",
      "line": 7,
      "fix": "    if (username.length() < 3 || username.length() > 15) {",
      "altFixes": [],
      "clue": "In Java, getting the length of a String requires calling a method, not accessing a field. Arrays use .length, but Strings are different."
    },
    {
      "id": "b2",
      "line": 12,
      "fix": "    if (!Character.isLetter(username.charAt(0))) {",
      "altFixes": [],
      "clue": "The first character of a String is at index 0, not index 1. This check is supposed to validate the very first character."
    },
    {
      "id": "b3",
      "line": 17,
      "fix": "    if (username.equals(banned)) {",
      "altFixes": [
        "    if (username.equalsIgnoreCase(banned)) {"
      ],
      "clue": "Comparing Strings with == checks if they are the same object in memory, not if they have the same text. Use the right String method."
    }
  ]
}
